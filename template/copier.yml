project_name:
  type: str
  help: What is your project name?

project_slug:
  type: str
  help: What is your project slug (package name)?
  default: "{{ project_name | lower | replace(' ', '-') }}"

project_description:
  type: str
  help: What is your project description?

module_name:
  type: str
  help: What is your Python module name?
